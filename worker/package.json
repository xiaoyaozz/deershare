{
  "name": "deershare-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "db:init": "wrangler d1 execute deershare-db --file=./migrations/001_init.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
