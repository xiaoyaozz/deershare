{
  "name": "deershare-cf",
  "version": "1.0.0",
  "description": "小鹿快传 - Cloudflare部署版",
  "scripts": {
    "install:all": "cd client && npm install && cd .. && cd worker && npm install",
    "build:client": "cd client && npm run build",
    "deploy:worker": "cd worker && npm run deploy",
    "deploy:db": "cd worker && npm run db:init"
  },
  "author": "",
  "license": "MIT"
}
